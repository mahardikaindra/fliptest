import * as ComponentsAction from "../collections/Components/actions";
import * as HomeAction from "../collections/Home/actions";

export const ActionCreators = Object.assign(
  ComponentsAction,
  HomeAction,
);
